import { ListingDetailConnected } from "@/components/connected/listing-traceability-connected";
import { AppPageShell } from "@/components/layout/app-page-shell";

export default function ListingDetailPage() {
  return (
    <AppPageShell>
      <ListingDetailConnected />
    </AppPageShell>
  );
}
