import { DeployReadinessConnected } from "@/components/connected/deploy-readiness-connected";
import { AppPageShell } from "@/components/layout/app-page-shell";

export default function DeployReadinessPage() {
  return (
    <AppPageShell>
      <DeployReadinessConnected />
    </AppPageShell>
  );
}
