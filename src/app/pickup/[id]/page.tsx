import { PickupRouteDetailConnected } from "@/components/connected/pickup-route-detail-connected";
import { AppPageShell } from "@/components/layout/app-page-shell";

export default function PickupDetailPage() {
  return (
    <AppPageShell>
      <PickupRouteDetailConnected />
    </AppPageShell>
  );
}
