import { PickupRoutesConnected } from "@/components/connected/collector-flows-connected";
import { AppPageShell } from "@/components/layout/app-page-shell";

export default function PickupRoutesPage() {
  return (
    <AppPageShell>
      <PickupRoutesConnected />
    </AppPageShell>
  );
}
