import { CheckoutConnected } from "@/components/connected/checkout-connected";
import { AppPageShell } from "@/components/layout/app-page-shell";

export default function CheckoutPage() {
  return (
    <AppPageShell>
      <CheckoutConnected />
    </AppPageShell>
  );
}
